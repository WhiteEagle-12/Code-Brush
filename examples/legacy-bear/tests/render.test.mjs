import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs/promises';import{createRequire}from'node:module';
import{configure,validateFilm}from'../src/core.mjs';import{createScene}from'../films/bear-waterfall/scene.mjs';
const require=createRequire(import.meta.url),{createCanvas,loadImage,Path2D}=require('@napi-rs/canvas');configure({createCanvas,Path2D});
test('film renders identical pixels after arbitrary seeks',async()=>{const film=JSON.parse(await fs.readFile(new URL('../films/bear-waterfall/film.json',import.meta.url)));assert.equal(validateFilm(film).frames,240);const bg=await loadImage(new URL('../films/bear-waterfall/assets/grotto.png',import.meta.url).pathname),scene=createScene(film,{background:bg}),c=createCanvas(480,270),ctx=c.getContext('2d');scene.render(ctx,4.3);const reference=Buffer.from(c.data());for(const t of[9.5,0,7.8,2.7,6.3])scene.render(ctx,t);scene.render(ctx,4.3);assert.deepEqual(Buffer.from(c.data()),reference);});
